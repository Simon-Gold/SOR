version: '3'
services:
  project-sor-api:
    build: .
    image: project-sor-api
    ports:
      - "${PROJECT_SOR_API_PORT:-5000}:5000"
    volumes:
      - type: volume
        source: data
        target: /data
    env_file: .env
    environment:
      DATABASE_URL: sqlite:////data/db.sqlite
    restart: always
volumes:
  data:
